# Requirements for the Streamlit app (the deployed delivery).
# This file is intentionally minimal so Streamlit Cloud builds stay fast
# and conflict-free. For the Dash dashboard, the NiceGUI Control Room,
# figure export, and the legacy PDF tooling, use:
#     pip install -r requirements-full.txt
streamlit>=1.10.0
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
sympy>=1.8.0
pandas>=1.3.0
plotly>=5.3.0
Pillow>=9.0.0
